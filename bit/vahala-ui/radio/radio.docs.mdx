---
description: An accessible, responsive, and customizable Radio component.
labels: ['vahala-ui', 'Radio', 'a11y']
---

import { FormControl, RadioGroup, FormLabel } from '@mui/material';
import { ThemeProvider } from '@vahalawebdesign/vahala-ui.themes.theme-provider';
import { Radio } from './radio';

The `<Radio />` component extends the [MUI Radio](https://mui.com/material-ui/react-radio-button/) including its theming, inline `sx` styling, and props.

By default the `Radio` is wrapped with a `FormControlLabel` MUI component, which has the required the `label` prop. All radios must have a label for accessibility compliance.

The `Radio` needs to be a child of the `RadioGroup`, which must contain two or more radio children. The `RadioGroup` must also have an MUI `FormLabel`, which has an `id` that matches the `RadioGroup` `aria-labelledby` prop.

#### Accessibility

The component is fully accessible and customizable: supporting mouse, keyboard, and touch interactions, focus behavior, and ARIA props.

For radio group a11y compliance and usage, visit [ARIA Authoring Practices Guide (APG)](https://www.w3.org/WAI/ARIA/apg/patterns/radio/).

### Component usage

```js
<FormControl>
  <FormLabel id="docs-radio-button-group-label">Docs Group</FormLabel>
  <RadioGroup
    aria-labelledby="docs-radio-button-group-label"
    name="docs-radio-button-group"
  >
    <Radio label="Radio 1" value="1" />
    <Radio label="Radio 2" value="2" />
    <Radio label="Radio 3" value="3" />
  </RadioGroup>
</FormControl>
```

### Render Radio

```js live
<ThemeProvider>
  <FormControl>
    <FormLabel id="radio-button-group-label">Docs Group</FormLabel>
    <RadioGroup
      aria-labelledby="radio-button-group-label"
      name="radio-button-group"
    >
      <Radio label="Radio 1" value="1" />
      <Radio label="Radio 2" value="2" />
      <Radio label="Radio 3" value="3" />
    </RadioGroup>
  </FormControl>
</ThemeProvider>
```
